import { Details, CustomLocations, SSL } from '../types';
export default function (url: string, token: string, Details: Details, CustomLocations: CustomLocations[] | null, SSL: SSL | null): Promise<object>;
